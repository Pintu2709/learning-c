#include <stdio.h>
int main (){
    int x=5;
    int y=3;
    /*Returns 1(true) because one of the condition are true (5 is grater than 3,but
    greater than 3,but 5 is not less than 4)*/
     
     printf()